<section class="section education-fact">
    <div class="container">
        <div class="row education-fact__row">
            <div class="col-12 col-sm-6 col-md-4">
                <div class="border-rectangle border-rectangle_bottom">
                    <img
                        src="{{ post.get_field('fact_image') }}"
                        alt="{{ post.get_field('fact_blockquote')|striptags }}">
                </div>
            </div>
            <div class="col-12 col-sm-6">
                <blockquote class="blockquote content">{{ post.get_field('fact_blockquote') }}</blockquote>
            </div>
        </div>
    </div>
</section>