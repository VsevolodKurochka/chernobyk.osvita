{% include "shared/consultation.twig" with {
    theme: theme,
    class: 'module-consultation_rocket',
    title: post.get_field('consultation-2-title'),
    subtitle: post.get_field('consultation-2-subtitle'),
    form: post.get_field('consultation-2-form')
} %}