{% extends "base.twig" %}

{% block content %}
	{% set sections = [
		'header', 'advantages', 'consultation-1', 'steps', 'who', 'features', 'reasons', 'schools', 'universities', 'consultation-2'
	] %}
	{% for section in sections %}
		{% include "home/#{section}.twig" %}
	{% endfor %}
{% endblock %}